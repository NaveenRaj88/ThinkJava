package com.initialization;

import static com.util.Print.*;
public class Bowl {

	Bowl(int marker) {
		print("Bowl(" + marker + ")");
		}
		void f1(int marker) {
		print("f1(" + marker + ")");
		}
		
	
}
